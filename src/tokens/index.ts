/**
 * Design Tokens
 *
 * Generated design tokens from Style Dictionary for consistent
 * styling across the UI Kit. These tokens provide a single source
 * of truth for colors, spacing, typography, and component styles.
 *
 * @example
 * ```tsx
 * import { ColorCoreNeutral900, SpacingCore4 } from 'ui-kit/tokens';
 * import tokensJson from 'ui-kit/tokens/generated/tokens.json';
 *
 * // Use individual tokens
 * const textColor = ColorCoreNeutral900;
 * const padding = SpacingCore4;
 *
 * // Or use the full tokens object
 * const allTokens = tokensJson;
 * ```
 */

// Export all generated tokens as named exports
export * from './generated/tokens';
