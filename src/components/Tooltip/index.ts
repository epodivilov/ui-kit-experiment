/**
 * Tooltip Component
 *
 * A tooltip component built with Base UI for accessibility and positioning.
 * Displays helpful information when hovering over or focusing on elements.
 *
 * @example
 * ```tsx
 * import { Tooltip } from 'ui-kit';
 *
 * <Tooltip content="This is a helpful tooltip" placement="top">
 *   <button>Hover me</button>
 * </Tooltip>
 * ```
 */

export { Tooltip } from './Tooltip';
export type { TooltipProps, TooltipPlacement, TooltipSize } from './Tooltip.types';