/**
 * Checkbox Component
 *
 * A flexible checkbox component built with Base UI for accessibility.
 * Supports different sizes, validation states, and indeterminate state.
 *
 * @example
 * ```tsx
 * import { Checkbox } from 'ui-kit';
 *
 * <Checkbox
 *   label="Accept terms and conditions"
 *   required
 *   helperText="Please read our terms and conditions"
 * />
 * ```
 */

export { Checkbox } from './Checkbox';
export type { CheckboxProps, CheckboxSize, CheckboxValidationState } from './Checkbox.types';