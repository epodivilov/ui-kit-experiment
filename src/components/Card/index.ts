/**
 * Card Component
 *
 * A flexible card component for content grouping with theme support.
 * Supports different variants, interactive states, and media content.
 *
 * @example
 * ```tsx
 * import { Card, CardTitle, CardSubtitle } from 'ui-kit';
 *
 * <Card variant="elevated" interactive>
 *   <CardTitle>Card Title</CardTitle>
 *   <CardSubtitle>Card subtitle</CardSubtitle>
 *   <p>Card content goes here...</p>
 * </Card>
 * ```
 */

export { Card, CardTitle, CardSubtitle } from './Card';
export type { CardProps, CardVariant, CardSize } from './Card.types';